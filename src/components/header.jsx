const Header = ({ title }) => {
    return (
        <header className="header-top">
            <ul>
                <li>{title}</li>
            </ul>
        </header>
    )
}

export default Header;